<p style="color: green"><%= notice %></p>

<!--h1>Tasks</!--h1 -->

<div id="tasks">
  <header>
      <h2 id="date"><%= l Date.today %></h2>
      <p id="task-count"></p>
        Total <%= @tasks.count %> Tasks Bellow:
      <%= render 'form', task: Task.new %>
  </header>
  <div id="list">
    <% @tasks.each do |task| %>
      <%= render task %>
    <% end %>
    </div>
</div>

<%# aqui criado pelo scaffold = link_to "New task", new_task_path %>
